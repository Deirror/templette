package button

templ Button(p Props) {
    attrs := p.AsTemplAttrs()

    if p.Href != "" {
        <a { attrs... }>
            { children }
        </a>
    } else {
        <button { attrs... }>
            { children }
        </button>
    }
}

