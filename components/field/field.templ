package field

import (
    "github.com/Deirror/templette/components/label"
    "github.com/Deirror/templette/props"
)

templ Field(p Props) {
    <div { p.AsTemplAttrs()... }>
        @label.Label(p.Label) {
            { p.Text }
        }

        { children... }
    </div>
}

